import { SerializedStyles } from 'jimu-core';
import { RowLayoutItemSetting } from '../../types';
export declare function getItemStyle(isMultiRow: boolean, sign: number, gutter: number, mergedStyle: any, itemSetting: RowLayoutItemSetting): [SerializedStyles, string];
